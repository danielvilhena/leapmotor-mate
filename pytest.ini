[pytest]
# The real test suite lives in tests/. Scope collection there so pytest never
# picks up app helper modules that happen to match its *_test.py / test_* naming
# and import runtime-only deps (e.g. paho) absent from the CI test env — which
# would otherwise break collection.
testpaths = tests
